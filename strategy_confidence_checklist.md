# Strategy Confidence Checklist

我不能对任何交易策略做“收益 100% 确信”的承诺。这里的 100% 只能定义为：在当前代码、数据和假设范围内，已系统排除已知工程漏洞和回测作弊路径。

## 必查漏洞与修复

| 漏洞 | 风险 | 本项目的修复 |
|---|---|---|
| LLM 直接下单 | 不可复现、不可审计、容易幻觉 | LLM 只输出候选 JSON，Freqtrade 策略不调用 LLM |
| 任意公式执行 | 候选公式可能执行恶意代码 | `src/factors.py` 使用 AST 白名单和空 `__builtins__` |
| 未来函数 | 回测收益虚高 | 只允许当前及历史 candle 指标，必须运行 `lookahead-analysis` |
| 递归指标不稳定 | EMA / ATR 启动窗口不足导致结果漂移 | `startup_candle_count=600`，必须运行 `recursive-analysis` |
| 过拟合参数 | 历史表现不可泛化 | 固定低维参数，先做样本外，再考虑优化 |
| 标签泄漏 | 用未来收益构造入场信号 | 未来收益只在 `evaluate_factors.py` 离线评估中出现，不进入策略 |
| 手续费和滑点忽略 | 频繁交易策略被成本吞掉 | 使用 Freqtrade exchange/config 回测，并比较交易次数和平均收益 |
| 只看收益率 | 回撤、亏损期不可接受 | 回测报告必须同时看回撤、交易次数、收益因子、最长亏损 |
| 单币种幸存偏差 | 只对 BTC 或 ETH 某一段有效 | 默认同时评估 BTC/USDT 和 ETH/USDT |
| 合约杠杆放大风险 | 亏损和爆仓不可控 | 默认 spot、dry_run、无做空 |
| API Key 泄漏 | 账户安全风险 | 配置文件留空，`.env.example` 不包含真实密钥 |

## 进入下一阶段的硬条件

1. 固定策略在 BTC 和 ETH 上均可完成回测。
2. `lookahead-analysis` 无未来函数问题。
3. `recursive-analysis` 无重大指标漂移。
4. 因子评估报告覆盖率充足，不能只依赖极少数样本。
5. 样本外表现没有完全崩塌。
6. dry-run 至少 2 到 4 周，订单行为与预期一致。

## 不应跳过的对照组

- Buy and hold BTC/ETH。
- 只用 EMA 趋势过滤的基线策略。
- 不使用 LLM 因子的固定策略。
- 低交易频率版本。

只有新策略在风险调整后稳定优于这些对照组，才值得继续投入。
